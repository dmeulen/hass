cloud:
  google_actions:
    filter:
      exclude_entities:
        - switch.garage_deur
        - switch.keuken_led
      exclude_domains:
        - group
    entity_config:
      switch.guest_switch:
        name: gast mode
        aliases:
          - oppas mode
          - guest mode
      switch.tv_kijken:
        name: de televisie
        aliases:
          - de TV
          - media flop
          - ziggo
      climate.toon_van_eneco:
        name: toon van eneco
        aliases:
          - verwarming
          - toon
